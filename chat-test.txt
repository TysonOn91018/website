============================================
同听聊天功能 - 测试步骤
============================================

一、配置 Supabase（只需做一次）
----------------------------------------------
1. 打开 https://supabase.com 注册/登录，新建一个项目。

2. 在项目里打开 [SQL Editor]，执行下面整段 SQL：

   create table chat_messages (
     id uuid default gen_random_uuid() primary key,
     room_id text not null,
     user_id text not null,
     user_name text not null,
     message text not null,
     created_at timestamptz default now()
   );

   alter publication supabase_realtime add table chat_messages;

3. 打开 [Settings] -> [API]，复制：
   - Project URL（例如 https://xxxxx.supabase.co）
   - anon public key（一长串字符串）

4. 在 index.html 的 <head> 里、<script src="./script.js"> 之前加一行（把 URL 和 key 换成你的）：

   <script>window.MOOD_PLAYER_SUPABASE={url:'https://你的项目.supabase.co',anonKey:'你的anon key'};</script>

   例如：
   <script>window.MOOD_PLAYER_SUPABASE={url:'https://abcdef.supabase.co',anonKey:'eyJhbGc...'};</script>


二、本地运行页面
----------------------------------------------
用本地服务器打开（不要直接双击 html）：

   npx serve .

然后用浏览器打开终端里显示的地址，例如 http://localhost:3000


三、模拟「两个人同时听同一首」
----------------------------------------------
1. 开两个窗口：
   - 窗口 A：正常打开 http://localhost:3000
   - 窗口 B：再开一个标签页，同样打开 http://localhost:3000
   （或用无痕/另一浏览器，模拟两个用户）

2. 在 窗口 A 和 窗口 B 里都：
   - 点同一个心情（例如「放松」），进入播放页。
   - 两边的当前曲目都是同一首（夜に溶けるまま / chill 01.mp3）。

3. 等 2～3 秒：
   - Supabase Presence 会同步，两边都会检测到「有 2 人在同一房间」。
   - 此时两边的「💬 Chat」按钮会从灰色变为可点。

4. 在 窗口 A 点「💬 Chat」：
   - 右侧滑出聊天面板。
   - 在输入框打字，点「发送」。

5. 在 窗口 B 也点「💬 Chat」：
   - 应能看到 窗口 A 刚发的消息。
   - 在 窗口 B 也发一条，窗口 A 里也会实时出现。

6. 点面板右上角 ✕ 可收起聊天。


四、若 Chat 一直是灰色
----------------------------------------------
- 确认 index.html 里已正确配置 MOOD_PLAYER_SUPABASE（url 和 anonKey 都不为空）。
- 确认两个窗口都在「播放页」且是同一首曲目（同一心情、同一 track）。
- 打开浏览器 F12 -> Console，看是否有 Supabase 或网络报错。
- 未配置 Supabase 时，按钮会一直灰并提示「配置 Supabase 后可启用同听聊天」。
